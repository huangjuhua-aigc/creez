# Build Creez Electron installers for Windows and Mac.
# Trigger: push to release branch, or manually via Actions tab ("Run workflow").
# Artifacts: after a run, open the workflow run's Artifacts section to download
#            "creez-installer-win" (Windows) and "creez-installer-mac" (Mac).
name: Build Electron (Creez)

on:
  push:
    branches: [release]
  workflow_dispatch:

jobs:
  build:
    runs-on: ${{ matrix.os }}
    strategy:
      fail-fast: false
      matrix:
        include:
          - os: windows-latest
            platform: win
          - os: macos-latest
            platform: mac

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: "20"
          cache: "npm"
          cache-dependency-path: src/package-lock.json

      - name: Install dependencies
        working-directory: src
        run: npm ci

      - name: Build app
        working-directory: src
        run: npm run build:${{ matrix.platform }}
        env:
          # Skip code signing when no certs are configured (avoids hang/failure).
          CSC_IDENTITY_AUTO_DISCOVERY: false

      - name: Upload installers
        uses: actions/upload-artifact@v4
        with:
          name: creez-installer-${{ matrix.platform }}
          path: |
            src/release/*.exe
            src/release/*.dmg
          if-no-files-found: error
